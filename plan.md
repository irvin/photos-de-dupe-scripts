# `checkimg-speed-dupe` 低速照片去重修正計畫

## 背景

目前執行：

```bash
checkimg-speed-dupe geocoded dupe 1
```

時，程式會依照片的 EXIF 拍攝時間與 GPS 座標計算移動速度，並將速度低於
`1 km/h` 的照片從 `geocoded/` 移至 `dupe/`。

現行演算法會把同一資料夾內的所有照片視為一條連續軌跡。當不同錄影片段之間
存在較長時間間隔時，程式仍會跨片段計算平均速度。若兩個片段的端點位置接近，
計算結果便可能低於速度門檻。

此外，現行向前回溯邏輯會持續使用同一張較晚照片與更早照片比較，導致一次錯誤
判定連鎖移除多張實際行車速度正常的照片。

## 修正目標

- 不跨越拍攝中斷點計算速度。
- 長時間停留但持續拍攝時，整個低速區段仍只保留最後一張照片。
- 所有速度判定均以原始相鄰照片為準。
- 不因移動照片而產生新的跨照片比較關係。
- 保持現有命令列參數與輸出路徑行為。

## 演算法

### 1. 讀取及排序照片

對每個包含 JPG 的資料夾：

1. 讀取照片的 EXIF 拍攝時間與 GPS 座標。
2. 缺少 EXIF 拍攝時間時，沿用現有行為，以檔案 `mtime` 作為備援。
3. 依拍攝時間由早至晚排序。
4. 保留排序後的原始相鄰關係，判定期間不移動檔案。

### 2. 切分連續序列

依排序後的原始相鄰照片切分序列，預設時間間隔門檻為 30 秒。

- 相鄰照片時間差大於 0 且不超過 30 秒：歸入同一序列。
- 相鄰照片時間差超過 30 秒：結束目前序列，下一張開啟新序列。
- 時間差等於或小於 0：視為無效時間關係並切分序列。
- 切分序列本身不移除任何照片。
- 禁止跨序列計算速度。

30 秒門檻只用來判斷原始相鄰照片之間是否發生拍攝中斷，不是照片保留間隔，
也不可套用在移除照片後形成的回溯比較上。

### 3. 判定低速區段

每個連續序列獨立處理：

1. 只比較序列中的原始相鄰照片。
2. 任一張缺少 GPS 時，不計算該相鄰組的速度，並中斷目前低速區段。
3. 使用 Haversine 距離與時間差計算相鄰照片間的平均速度。
4. 若速度低於命令列指定門檻，將較早照片標記為待移除。
5. 若速度達到或超過門檻，結束目前低速區段，兩張照片均不因該相鄰組而移除。

例如：

```text
A -> B：0.2 km/h
B -> C：0.3 km/h
C -> D：10 km/h
```

處理結果：

- `A -> B` 低速，標記 `A`。
- `B -> C` 低速，標記 `B`。
- `C -> D` 為正常速度，不標記 `C`。
- 最終移除 `A`、`B`，保留低速區段最後一張 `C`，並保留 `D`。

### 4. 長時間停留

只要相鄰照片持續在 30 秒內拍攝，即使停留時間超過 30 秒，仍屬於同一連續序列。

例如每 5 秒拍攝一張且持續靜止數分鐘：

```text
00s -> 05s -> 10s -> ... -> 180s
```

每個原始相鄰組的時間差均為 5 秒，因此不會切分序列。若所有相鄰組的速度均低於
門檻，最終只保留 `180s` 的最後一張照片，不會每 30 秒保留一張。

### 5. 拍攝中斷

若原始相鄰照片超過 30 秒：

```text
A：00s
B：45s
```

則在 `A` 與 `B` 之間切分序列：

- 不計算 `A -> B` 的速度。
- 不因這個時間間隔移除 `A` 或 `B`。
- `B` 作為新序列的第一張照片繼續處理。

### 6. 移動檔案

完成整個資料夾的判定後，才統一移動被標記的照片：

1. 將待移除照片由輸入資料夾移至輸出資料夾。
2. 保留照片相對於輸入根目錄的路徑。
3. 判定期間不檢查照片是否已被移動，也不重新計算相鄰關係。
4. 若輸出位置已有同名檔案，應回報錯誤或明確跳過，不可靜默覆寫。

## 執行摘要

每個資料夾完成後輸出：

- 掃描的照片數量。
- 切分出的連續序列數量。
- 因時間間隔超過 30 秒而切分的次數。
- 因無效時間或缺少 GPS 而無法判定的相鄰組數量。
- 標記及實際移動的照片數量。

## 驗證計畫

### 使用現有 `testing-data`

1. 複製 `testing-data/geocoded/` 到暫存輸入資料夾。
2. 使用速度門檻 `1 km/h` 執行修正後的程式。
3. 確認不同錄影片段之間不再進行速度比較。
4. 確認目前被誤移至 `testing-data/dupe/` 的 190 張正常行車照片不再因跨片段或
   回溯比較而被移除。

### 回歸案例

至少加入下列測試案例：

1. **正常行車**
   - 相鄰照片間隔不超過 30 秒。
   - 計算速度高於門檻。
   - 預期不移除任何照片。

2. **連續靜止超過 30 秒**
   - 每 5 秒拍攝一張，總停留時間超過 30 秒。
   - 所有相鄰速度低於門檻。
   - 預期只保留整段最後一張。

3. **拍攝中斷**
   - 兩張相鄰照片相隔超過 30 秒。
   - 即使兩張位置相同，也不得互相比較或移除。

4. **低速後恢復行車**
   - 連續數組低速相鄰照片後，速度恢復正常。
   - 預期低速區段只保留最後一張，正常速度照片全部保留。

5. **缺少 GPS**
   - 序列中包含缺少 GPS 的照片。
   - 預期不跨過缺少 GPS 的照片建立新的速度比較。

6. **無效或相同時間**
   - 相鄰照片時間相同或倒序。
   - 預期切分序列且不移除照片。

7. **重複執行**
   - 對第一次執行後的輸入資料再次執行。
   - 預期不再因新形成的非原始相鄰關係移除額外照片。

## 完成條件

- 現有命令 `checkimg-speed-dupe geocoded dupe 1` 可正常執行。
- 不跨越 30 秒以上的原始拍攝間隔計算速度。
- 長時間連續靜止只保留低速區段最後一張。
- 不會因先移動照片而連鎖誤刪正常行車照片。
- 所有回歸案例通過。
